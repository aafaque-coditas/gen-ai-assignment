# Payments service notes

On-call rotation lookups should go through the `oncall` MCP server, not a
hard-coded spreadsheet link -- see ../../oncall_server.py.

TODO: link the real runbook here once it's written.
